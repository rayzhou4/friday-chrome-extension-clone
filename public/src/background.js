/* background.js removed — popup-based auth is used and this file kept as placeholder.
   Original background/service-worker code has been moved into `src/modules/auth.js` and
   popup now performs the OAuth flow via chrome.identity.launchWebAuthFlow.
*/
